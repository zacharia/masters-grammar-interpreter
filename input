#conventions:
#returns a list of node objects
#takes a single argument, which is the parent node
#defines a Node object with name axiom. This is the axiom of the grammar

axiom = Node("start")

def start(parent):
    ret = Node("one")
    ret.setSymmetry("rotational", math3D.zero3(), math3D.zero3(), 2)
    return [ret]

def one(parent):
    ret = Node("two")
    
    return [ret]

def two(parent):
    ret = Node("two")
    return [ret]
