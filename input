#conventions:
#returns a list of node objects
#takes a single argument, which is the parent node
#defines a Node object with name axiom. This is the axiom of the grammar

axiom = Node("thing")

def thing(parent):
    ret = Node("thing")
    bleh = Node("bleh")
    ret.setSymmetry("reflective", math3D.zero3(), math3D.zero3())
    return [ret, bleh]

def bleh(parent):
    ret = Node("banana sundae")
    return [ret]

