#conventions:
#returns a list of node objects
#takes a single argument, which is the parent node
#defines a Node object with name axiom. This is the axiom of the grammar

axiom = Node("start")

def start(parent):
    ret = Node("one", in_position=(1.0, 0.0, 0.0), in_orientation=math3D.fromAngleAxisQ(90, 0,1,0))
    ret.setSymmetry("reflective", math3D.zero3(), (1.0, 0.0, 0.0))
    return [ret]

def one(parent):
    ret = Node("two")
    
    return [ret]

def two(parent):
    ret = Node("two")
    return [ret]
